# 09 · 版本历史（Changelog）

## v0.4.6（2026-08-29）

- 审计修复：`officialDshVersion` 先 realpath 解链再探测（pnpm 符号链接安装形态下
  版本漂移检测失效的问题）；扫描器 eval 补信任边界注释；
- 工程化：新增 `.gitignore` / `LICENSE`，清理 `.DS_Store` 残留；
- 文档：wiki 按 DSH-MacOS 结构重写为 9 章，README 对齐其惯例；开源发布。
## v0.4.5（2026-08-29）

- 插件注释全量实装：插件列表 169 个可见插件全部收录（154 条词典 + 别名映射），
  译文依据各包 package.json 官方英文原文校准；
- 修复描述串台：独占容器判定（某层恰 1 个已知名=卡片边界，≥2 个=越界拒绝）；
- 已停用插件注入支持（配置状态行双锚点）。

## v0.4.3 – v0.4.4（2026-08-29）

- 修复插件注释不渲染：官方卡片为 dl 网格布局，描述改为直接插入 dt/dd 对；
- 按真实 DOM（li>cardDetails>dl>div>dt+dd）重写注入器。

## v0.4.0 – v0.4.2（2026-08-29）

- 新增插件列表描述注入功能（pluginInfo 词典层 + 宿主路由透传 + 客户端注入器）；
- 徽章术语对齐 ZCode 惯例（用户消息/助手消息/系统提示词/工具调用）；
- 新增 Provider→模型供应商、Tool calls→工具调用次数、Duration/Timing source/
  Session timestamps/Hierarchy 等轨迹详情词条；
- 技能描述改菜单短译（修复溢出）。

## v0.3.0 – v0.3.3（2026-08-29）

- 新增应用内开关（设置→通用设置→语言行左侧，热启停 + localStorage 持久化）；
- 修复真实环境注入失败：官方 Menu 组件 span 包裹层适配 + insertBefore 竞态重试；
- 全量英文扫描补录：轨迹徽章（ASSISTANT/USER/TOOL/COMPACTED）、Turn/Step/Request
  计数模式、详情面板全字段、耗时页签、/model 描述、Session log 按钮、权限预设名。

## v0.2.0（2026-08-29）

审计 13 项修复全落地：

- 内存：登记最小化 + removedNodes 即时注销（长会话不增长）；
- 安全：直达路径补 SKIP_TAGS/contentEditable 父级检查；用户气泡硬护栏
  （`data-chat-flow-kind="user"`）；
- 修复 publish 自递归（原实现靠 try/catch 吞栈溢出）；
- 韧性：词典 localStorage 首屏缓存（消除英闪）、拉取失败退避重试、
  `/status` 版本漂移回声、宿主 register 护栏；
- 自愈：官方补译让位（CJK 探测）；
- 性能：三段式查词（literals O(1) → chrome 惰性 closest → patterns 预筛单遍）；
- 修复扫描器空参崩溃、pack.sh 词典校验盲区、产物混入 .DS_Store；
- 自测 39 → 84 项；新增浏览器真实模拟推演（tests/simulation/，38 项）。

## v0.1.0（2026-08-25）

首个可用版本：

- 双层翻译架构（键级覆盖 + 字面量/模式映射 + chrome 作用域）；
- 宿主词典路由（现读磁盘、按文件序合并、坏文件隔离）；
- 卸载逐字节还原；词典热更新；离线自测 35 项；
- 官方包缺口扫描器、三份调研报告、初版 wiki。
